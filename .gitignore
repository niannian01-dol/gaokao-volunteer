# 本地 Node 运行时（自带一份 Node + wrangler，约 270MB，不要提交）
nodejs/
node_modules/

# wrangler 本地状态：本地 D1 数据库文件、缓存、登录凭据
.wrangler/
.dev.vars
.env
.env.*

# 临时目录与数据库文件
work/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# 真实数据的导入脚本（体积大、且来自各省考试院，有版权风险，不要提交进公开仓库）
schema/03_import.sql
schema/04_segments.sql

# 编辑器 / 操作系统
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
